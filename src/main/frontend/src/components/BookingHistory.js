import React from 'react';

function BookingHistory() {
    // Logic lấy và hiển thị lịch sử đặt lịch từ backend
    return (
        <div>
            <h2>Lịch sử đặt lịch</h2>
            {/* Hiển thị lịch sử đặt lịch */}
        </div>
    );
}

export default BookingHistory;